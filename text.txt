Here is a highly detailed, copy-ready prompt you can feed to an AI to generate your new invitation code.

---

Act as an expert frontend developer and UI/UX designer. I need a single-file HTML, CSS, and Vanilla JS wedding anniversary invitation based exactly on the design, typography, and color palette shown in the reference image "NewAnivarsary.jpg".

### Core Architecture & Global Rules

* **Fully Responsive**: The design must flawlessly adapt to both mobile and desktop screens. Do not include any landscape orientation locks or warnings.


* **Strict Dependency Limits**: Do not use Bootstrap, Tailwind, jQuery, or any third-party libraries except **Embla Carousel** for the slider.


* **SVG Only**: All icons (music player, schedule timeline, UI elements) must be written as 100% inline `<svg>` elements. Do not use FontAwesome or web fonts.


* **Canvas Background**: Implement an HTML5 `<canvas>` element behind the main content that animates minimal, gently swaying floating leaves (using `requestAnimationFrame`).



### Entry Animation & Global Features

* **Pure CSS Envelope**: Create a full-screen entry overlay featuring a CSS-only envelope. On load, the page scroll must be locked (`overflow: hidden`). When the user taps anywhere, trigger a CSS animation of the envelope opening, fade out the overlay, and unlock the body scroll.


* **Hero Section**: Replicate the exact typography, organic blob shapes, and layout of the top section in "NewAnivarsary.jpg". Once the envelope animation finishes and scrolling is unlocked, display a small, bouncing inline SVG scroll-down arrow that automatically hides when the user scrolls past 50px.


* **Floating BGM Control**: Place a fixed Floating Action Button (FAB) at the bottom right with inline SVG muted/unmuted icons. It should handle background music and bypass autoplay restrictions by starting on the user's first interaction.


* **Scroll Animations**: Use `IntersectionObserver` to add smooth fade-in and slide-up reveal animations for every section as it enters the viewport.



### Content Sections & Form

* **Our Journey (Embla Carousel)**: Replace the static illustrated grid from "NewAnivarsary.jpg" with an interactive Embla Carousel. It must auto-play, support touch/swipe, and use real placeholder photos of a couple rather than illustrations.


* **Schedule & Venue**: Recreate the wavy timeline for the schedule and the aesthetic for the venue and note sections perfectly matching the image.
* **Redesigned RSVP**: Create a beautiful, responsive form including fields for Name, Phone, Guest Count, Dietary Preferences, and a Message. Use Vanilla JS to capture this data, format it into a text message, and dispatch it directly to a WhatsApp API link on submission.


* **Mandatory Footer**: The bottom of the page must contain the exact credit line: `Designed by <a href="https://invitebox.in/" target="_blank">Invite&nbsp;Box</a>`.

